{
  "name": "marketmakeragent",
  "version": "1.0.0",
  "description": "Routes natural-language task intents to agents using ERC8004 semantic search.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "test": "node --import tsx --test tests/*.test.ts",
    "test:live": "node --import tsx --test tests/live/*.live.test.ts",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260206.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "wrangler": "^4.63.0"
  }
}
